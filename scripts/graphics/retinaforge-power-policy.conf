# Copied to /etc/retinaforge/power-policy.conf by install-work-power.sh.
# This machine is a desk workstation (not used on a commute).
# 0 = never auto-sleep the GT 750M (default). Native HDMI/TB need it.
# 1 = also sleep it on battery when no extra display is connected.
COMMUTE_SLEEP=0
