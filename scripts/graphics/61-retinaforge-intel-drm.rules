# Stable DRM node for Iris Pro (PCI 00:02.0). Hyprland AQ_DRM_DEVICES cannot
# use /dev/dri/by-path because those names contain colons.
KERNEL=="card[0-9]*", KERNELS=="0000:00:02.0", SUBSYSTEM=="drm", SUBSYSTEMS=="pci", SYMLINK+="dri/intel-igpu"
